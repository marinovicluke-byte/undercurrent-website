// components/pages/LocationPage.js
// Stub — full implementation in Task 4
export default function LocationPage({ location }) {
  return (
    <main>
      <h1>{location.metaTitle}</h1>
    </main>
  )
}
